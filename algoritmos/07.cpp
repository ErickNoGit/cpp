#include <iostream>
#include <iomanip>

int main()
{
    int DIFERENCA, A, B, C, D;
    A = 5, B = 6, C = 7, D = 8;
    DIFERENCA = (A * B - C * D);
    std::cout << "DIFERENCA = " << DIFERENCA;

    return 0;
}